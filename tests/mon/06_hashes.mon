let person = {"name": "Alice", "age": 30, "active": true};
let numbers = {1: "one", 2: "two", 3: "three"};

let name = person["name"];
let age = person["age"];
let one = numbers[1];

if (person["active"]) {
    age + 10
} else {
    0
}
